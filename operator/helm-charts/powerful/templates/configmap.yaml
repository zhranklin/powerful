apiVersion: v1
kind: ConfigMap
metadata:
  name: powerful-cases
  namespace: {{ include "namespace" .}}
  labels:
    release: {{ .Release.Name }}
data:
  config.yaml: |-
    targetMappings:
{{ .Values.targetMappings | toYaml | indent 6 }}
