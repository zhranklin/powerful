# How to build base image:
# ./build-base-img.sh
FROM zhranklin/toolbox:v0.1.0
ENV OPERATOR=/usr/local/bin/helm-operator \
    USER_UID=1001 \
    USER_NAME=helm \
    HOME=/opt/helm
COPY build/_output/bin/helm-operator ${OPERATOR}
